spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/jpashop?useUnicode=true&characterEncdoing=UTF-8&serverTimezone=UTC
    username: root
    password: admin

  jpa:
    hibernate:
      ddl-auto: create-drop   # 마지막에 drop 쿼리를 날려줌 (깨끗해진다?)
    properties:
      hibernate:
#        show_sql: true
        format_sql: true

logging:
  level:
    org.hibernate.SQL : debug
    org.hibernate.type : trace